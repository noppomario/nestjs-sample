/**
 * 定数定義
 */
export class Constants {
  /**
   * DB接続用の識別子(users)
   */
  static readonly DB_CONNECTION_USERS = 'userdb';

  /**
   * DB接続用の識別子(logs)
   */
  static readonly DB_CONNECTION_LOGS = 'logsdb';
}
